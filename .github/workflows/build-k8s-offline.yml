name: Build K8s Offline Installer

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
    paths:
      - ".github/workflows/build-k8s-offline.yml"
      - "src/04-k8s/**"
  pull_request:
    paths:
      - ".github/workflows/build-k8s-offline.yml"
      - "src/04-k8s/**"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: src/04-k8s
    strategy:
      fail-fast: false
      matrix:
        arch:
          - amd64
          - arm64
        bundle:
          - full
          - lite

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Sealos downloads
        uses: actions/cache@v4
        with:
          path: src/04-k8s/.cache/downloads
          key: sealos-downloads-${{ runner.os }}-${{ matrix.arch }}-${{ hashFiles('src/04-k8s/common/component-versions.env') }}
          restore-keys: |
            sealos-downloads-${{ runner.os }}-${{ matrix.arch }}-

      - name: Ensure executable permissions
        run: |
          chmod +x build.sh install.sh

      - name: Build package
        run: |
          ./build.sh --arch "${{ matrix.arch }}" --bundle "${{ matrix.bundle }}" --force

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: k8s-sealos-${{ matrix.arch }}-${{ matrix.bundle }}
          path: |
            src/04-k8s/dist/k8s-sealos-linux-${{ matrix.arch }}-${{ matrix.bundle }}.run
            src/04-k8s/dist/k8s-sealos-linux-${{ matrix.arch }}-${{ matrix.bundle }}.run.sha256
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-24.04

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
